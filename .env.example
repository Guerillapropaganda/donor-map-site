# Donor Map Research Pipeline — API Keys
# Copy this file to .env and add your keys.
# .env is gitignored — never commit real API keys.

# ── Core APIs (Politicians & Donors) ──────────────────

# FEC API (https://api.open.fec.gov/developers/)
# Free registration: 1,000 requests/hour (vs 40/hr with DEMO_KEY)
FEC_API_KEY=DEMO_KEY

# Congress.gov API (https://api.congress.gov/sign-up/)
# Free registration: 5,000 requests/hour
CONGRESS_API_KEY=DEMO_KEY

# Senate LDA API (Lobbying Disclosure)
# Default key is public — replace with your own if needed
LDA_API_KEY=b3e00f77b9db54cd753ca43bb8773f9e8b0ec5c4

# ── Think Tanks & Nonprofits ──────────────────────────

# ProPublica Nonprofit Explorer — NO KEY NEEDED
# 990 tax filings: revenue, exec pay, grants, donors
# Search: https://projects.propublica.org/nonprofits/api/v2/search.json?q=Heritage+Foundation
# Detail: https://projects.propublica.org/nonprofits/api/v2/organizations/{EIN}.json

# ── Federal Contracts ─────────────────────────────────

# SAM.gov — federal contract awards, entity registrations
# Free registration: https://sam.gov/content/home
# Shows which companies win government contracts
SAM_API_KEY=

# ── Foreign Lobbying ──────────────────────────────────

# FARA (DOJ Foreign Agents Registration) — NO KEY NEEDED
# Heavily rate-limited (429 errors common). Use sparingly.
# https://efile.fara.gov/

# ── Bill Tracking ─────────────────────────────────────

# GovTrack — NO KEY NEEDED
# Enhanced bill tracking, vote analysis, legislator stats
# https://www.govtrack.us/developers/api

# ── DISCONTINUED (do not use) ─────────────────────────
# OpenSecrets API — shut down public access
# FollowTheMoney — merged into OpenSecrets, API dead
# ProPublica Congress API — deprecated, use Congress.gov

# ── Bulk Data Downloads (manual) ──────────────────────
# FEC Bulk: https://www.fec.gov/data/browse-data/?tab=bulk-data
# IRS 990s: skip this — ProPublica API serves the same data as clean JSON
# FPPC (CA): https://www.fppc.ca.gov/transparency/campaign-finance.html
