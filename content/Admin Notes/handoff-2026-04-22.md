---
title: "Handoff to next session — 2026-04-22"
type: admin-note
status: open
lane: code
date: 2026-04-22
---

# Handoff to next session

Read this before starting. Also read:
- `content/Admin Notes/session-log-2026-04-21.md` — yesterday's full retro
- `content/Decisions/0017-data-complete-tier.md` through `0020-*`
- Run `/preflight` first (it'll parse the vault + check deploy status)

## Immediate state as of 2026-04-21 end

**Repo state:** v4 HEAD = `e8d43e74f`. Everything committed, nothing in flight.

**Public site state:** Only the 9 `data/public-routes.json` flagship slugs are exposed. The kill-switch `TIER_GATED_PUBLISHING = false` in `quartz/constructionMode.ts` prevents the 446 data-complete profiles from being publicly accessible. Rendering is ready; David has NOT yet signed off on flipping the switch.

**Local preview:** `npx quartz build` produces correct output. Start a `static-preview` server on port 8095 to view at desktop width (>800px for tabs; <800px shows accordion mode).

**Data state:**
- 446 profiles at `data-complete` (0 at `verified` — requires David sign-off)
- Canonical stores refreshed via yesterday's enrichment sprint (2026-04-21, 42 min)
- Some ingests failed non-blocking (fec-indiv-aggregate OOM, fec-oppexp partial, fec-pac-summary + fec-oth-bulk + voteview-bulk missing data paths)

## The highest-leverage next moves (ranked)

### 1. Flip the kill-switch when David is ready (5 min + 1 deploy)

```
# In quartz/constructionMode.ts, line ~81:
const TIER_GATED_PUBLISHING = true   // was: false
```

Commit + merge to v4. Deploy takes ~5 min. 446 profiles become publicly accessible. Before flipping:
- Spot-check 10+ profiles at `localhost:8095` across profile types (mega-donor, politician, cabinet, PAC, think-tank, corp)
- Confirm no rendering regressions visible
- Swap the "Building" pill on the landing page for "Live beta" or equivalent (minor tweak in `quartz/components/LandingPage.tsx` line ~120, currently hardcoded to "Building")
- Consider temporarily enabling "Browse by sector" link cards on the landing page (would need implementation — ~20 lines JSX)

### 2. Retry the OOM'd indiv-aggregate ingest (1-2 hours)

```bash
NODE_OPTIONS="--max-old-space-size=16384" node scripts/ingest-fec-indiv-aggregate.cjs
```

This processes the 19 GB FEC individual contributions bulk. Yesterday's attempt OOM'd at 11 min with 3.2 GB heap. Bumping to 16 GB should finish in ~30-45 min. Then:

```bash
node scripts/aggregate-indiv-to-edges.cjs
node scripts/reclassify-readiness.cjs --diagnose --write
```

Expected data-complete delta: small (+5-15) because most profiles max'd on other criteria, but individual-contribution queries through the Ask engine get more accurate.

### 3. Implement ADR-0019 (R2 bulk storage) — half-day to full-day

Unblocks derived-file untracking and lets CI regenerate derived stores on deploy. 13-step implementation in the ADR. Start:

1. Get David to create the Cloudflare R2 bucket `donor-map-bulk`
2. Generate read + write scoped API tokens
3. Add the 4 env vars to `.env.local` + GitHub Secrets
4. Write `scripts/lib/bulk-storage.cjs` (S3-compat wrapper)
5. Write `scripts/fetch-bulk.cjs` + `scripts/upload-bulk.cjs`
6. Initial 60 GB upload from David's machine (overnight)
7. Update one ingest script as the reference pattern
8. Roll to the other ~20 ingest scripts

### 4. Implement ADR-0020 (enrichment cadence) — ~2 hours

Short version: add a dispatcher entry to run `scripts/enrichment-sprint.sh` weekly (Sunday 02:00 Chicago), plus the auto-verification hook (reclassify + eval harness + integrity audit), plus admin-note generation on failures. Full 5-step checklist in the ADR.

### 5. Data-source gap fills (long-tail, ranked by ROI)

User requested a data-source guide; it's in the end-of-session chat. Condensed version:

- **Plum Book** (plumbook.gov) → Cabinet + political-appointee pipeline. 1-2 days. Unlocks ~113 currently-isolated Cabinet/agency profiles.
- **Wikidata SPARQL** → biographical backfill for Cabinet / SCOTUS / Governors. 2-3 days. Fills the "Who They Are" prose that no federal bulk has.
- **CA + NY state campaign finance** (cal-access.sos.ca.gov + elections.ny.gov) → ~1 week each. Covers ~15% of state-pol profiles combined.
- **Federal Judicial Center** (fjc.gov/history/judges) → judge biographies + appointment history. 1-2 days.
- **Other states** — project per state, accept long tail.

See yesterday's chat for full URL table.

## Infrastructure you have today (inventory)

### Scripts you wrote yesterday

- `scripts/reclassify-readiness.cjs` — 5-tier system, `--diagnose` shows which criteria each profile fails
- `scripts/backfill-last-enriched.cjs` — stamp git-mtime on never-enriched profiles
- `scripts/backfill-committee-assignments.cjs`
- `scripts/backfill-fec-candidate-totals.cjs`
- `scripts/backfill-bills-sponsored.cjs`
- `scripts/backfill-donor-spend-totals.cjs`
- `scripts/harvest-profile-sources.cjs` — ID-based canonical URL citations
- `scripts/harvest-edge-citations.cjs` — parameterized FEC/IRS search URL citations
- `scripts/enrichment-sprint.sh` — 25-step wrapper (6 failures yesterday, note below)
- `scripts/eval-query-engine.cjs` — 28 golden queries against live stores
- `scripts/profile-rendering-regression-tests.cjs` — 16 tests, post-build coverage

### Rendering stack you shipped

- `quartz/plugins/transformers/wrap-profile-sections.ts` — wraps auto-blocks + H2/H3 editorial sections into `.profile-section-card` divs with `data-tab` attributes
- `quartz/components/ProfileTabs.tsx` — client-side tab nav (Financials label, donor-subtype detection, smart default-tab selection)
- `quartz/components/ProfileHeader.tsx` — emits `data-profile-type` for all publishable types
- `quartz/components/ProfileTOC.tsx` — per-profile sidebar TOC grouped by tab
- `quartz/components/DataCompleteBanner.tsx` — built but REMOVED from layout (David's call). Kept for potential re-introduction.

### Sprint failures to investigate (non-blocking)

```
fec-pac-summary    — ENOENT on bulk dir (path hardcoded wrong? verify)
fec-oppexp-bulk    — died partway through cycle 10 (oppexp10.zip? corrupt?)
fec-oth-bulk       — missing oth00.zip
fec-indiv-aggregate — OOM at 11 min (need 16 GB heap)
voteview-bulk      — failed on startup (check legislator-positions/ path)
aggregate-indiv-to-edges — failed because indiv-aggregate upstream failed
```

Each one: check the specific log snippet in `/tmp/enrichment-*.log` (gone now — would need to re-run sprint to capture fresh logs).

## Things to NOT do

1. **Don't flip the kill-switch without David's explicit sign-off.** Once flipped, pulling back is a reversible but visible regression.
2. **Don't touch `data/relationships.jsonl` or `data/derived/*.jsonl` without running the sprint first** — you'd overwrite yesterday's fresh canonical state.
3. **Don't promote any profile to `verified`** — that's David-only per rule 13 + rule 9.
4. **Don't re-enable `DataCompleteBanner`** — David explicitly killed it. If the tier story needs a visible marker later, design fresh, don't resurrect that component.
5. **Don't run all 25 sprint steps blindly** if you're short on time. The 3-4 hour runtime is only justified when bulk data is genuinely stale (check file mtimes in `data/bulk/` + `C:/donor-map-data/bulk/`).

## Rules reminders

- **Rule 1:** canonical stores are source of truth. Frontmatter is cache.
- **Rule 4:** AI translates, never generates. Every fact traces to a source record.
- **Rule 10:** public exposure is explicit. Kill-switch is currently `false`.
- **Rule 13:** URL verification is Editor-only. Don't substitute, hunt, or repair URLs.
- **No em dashes** in profile content. Use periods or colons.
- **Class Analysis mandatory** for verified tier; optional for data-complete.

## Quick command reference

```bash
# Start of session
/preflight

# Diagnose current state
node scripts/reclassify-readiness.cjs --diagnose

# Run all evals
node scripts/eval-query-engine.cjs

# Run regression tests
node --test scripts/profile-rendering-regression-tests.cjs

# Rebuild + serve locally
rm -rf quartz/.quartz-cache && npx quartz build
node .claude/static-server.cjs 8095   # or use preview_start "static-preview"

# Force enrichment sprint
bash scripts/enrichment-sprint.sh

# Flip kill-switch (when David approves)
# Edit quartz/constructionMode.ts line ~81: TIER_GATED_PUBLISHING = true
```

## Commits landed yesterday (chronological, all on v4)

`fd3caf1ca` `9de9b777b` `21c497528` `73da39324` `4f35af7de` `370d1e342`
`1e89071f4` `95bc3efad` `00072ccc3` `b15489a4c` `2d334fe4c` `5fb4f1546`
`5d82d2c20` `0a130a19f` `ca8e0bbfe` `bcfaab134` `24a5a9e09` `f13adddd6`
`b1f76b1e4` `647349f36` `91758ae7e` `77304517f` `a4e07db71` `dc25f3e91`
`92b490837` `99c9e6d1d` `b262270cf` `ccf14a650` `9a7df4ff7` `92bec0eca`
`875e2332f`

~31 commits. Full descriptions in `git log v4 --oneline --since="2026-04-21"`.
