---
name: Ops App Audit — Running Problems Log
type: audit
status: open
owner: Code Claude
started: 2026-04-23
context: |
  David's vault has an estimated 500-2,000 data/content issues. Audit scripts surface
  real bugs every time. David's reaction: "this whole system isn't working."

  New mandate: refine the Ops app (localhost:3333) tab-by-tab so problems SHOW UP
  visibly. Normie-friendly. No one-off audit scripts.

  David's additional guidance this session:
    - Checklists lie in both directions (false positives + false negatives).
      He has to come to Claude to verify the verifier.
    - Bug queue doesn't auto-clear when Claude fixes bugs. Discipline problem.
    - Pipelines/scripts aren't running right — so everything downstream
      (readiness checks, freshness, signoff gates) is built on bad ground.
    - URL backlog is too much to triage manually.
    - "Everything feels off or broken" — he can't sign off on things he doesn't trust.
    - April 30 launch is NOT a hard deadline. Work at quality pace.

  Method: go page-by-page, no code changes without sign-off. Record every
  problem observed into this doc as we go. At end, triage together.
---

# Ops App Audit — Running Problems Log

Started: 2026-04-23 14:30 CDT
Status: in progress

Format: one entry per problem. Severity tags:
- **BLOCKING** — lies to David / signals wrong
- **STALE** — references dead concepts / obsolete data
- **ORPHAN** — code that runs against things that no longer exist
- **UX** — normie-confusing but not incorrect
- **DISCIPLINE** — Claude / workflow problem, not code problem

---

## Confirmed problems (before page-by-page trace)

### P-001 (DISCIPLINE): Bugs don't auto-resolve when Claude fixes them
Bug queue at `content/Admin Notes/bug-queue.md` + deferred items accumulate.
Claude writes fixes but doesn't mark the tracked entry resolved. Result: `/bugs`
page shows 267+ deferred items that may all be stale. David: "I dont want to have
to resolve check them myself. I want it automated."
Saved to memory as `feedback_bug_auto_resolve.md`. Going forward, Claude marks
resolved in the same commit as the fix.

### P-002 (BLOCKING): Checklist verdicts drift from reality
Both sign-off queue and launch-50 checklists report false positives AND false
negatives. David has to ask Claude to re-verify the verifier on every sign-off
attempt. Trace in progress to find exactly why.

### P-003 (UX): `/signoff-launch` is not in the sidebar
Launch 50 tracker has no nav entry. Confirmed by reading `ops/src/components/Sidebar.tsx`.
David has to type the URL to get there. Not urgent since April 30 isn't a hard
deadline, but surfaces the broader issue: sidebar has 30 entries but misses this one.

### P-004 (UX): Six different pages answer "what should I look at today?"
`/attention`, `/alerts`, `/notes`, `/bugs`, `/signoff-queue`, `/signoff-launch`
each surface a different slice of "pending work." Each hits a different data
source. Normie has to know which to open first. Candidate for consolidation
once the underlying signals are trustworthy.

### P-005 (STALE risk): `/bugs` page surfaces 267 deferred items from Phase-6
Phase-6 is explicitly closed per `ADR-0005`. These items may all be obsolete.
Needs a pass to confirm what (if anything) is still relevant.

### P-006 (UX): Alerts page uses localStorage for "resolved" state
`/alerts/page.tsx:50,82-87` — resolved set lives in browser localStorage.
Clearing browser data or using another device loses triage history. Should
be persisted server-side if triage state matters.

---

## Page-by-page findings

### /signoff-queue trace — the big lie

**Data flow:**
1. `/signoff-queue` page reads `/api/vault` which parses every profile.
2. Page filters profiles where `auditAPlusPassed` is truthy AND `lastVerifiedBy !== "editorial"`.
3. `auditAPlusPassed` is frontmatter field `audit-a-plus-passed`, stamped by `scripts/pipeline-janitor.cjs --tier=a-plus --cohort --write`.

**The lie (P-007 BLOCKING):** the stamp means completely different things for different profile types.

The janitor's A+ quality checks (`pipeline-janitor.cjs:284-351`) are gated on `type === 'politician'`:
- committee-relevant regulatory cross-ref
- 3+ Tier 1 source types
- automated legal-review pass (defamation-prone phrases outside blockquotes)
- both-sides entity detection
- `central-thesis` populated
- `story-grade` populated

For donors, corporations, and everything else, NONE of those checks run.

For non-politicians, the stamp fires when just three trivially-weak conditions pass:
- no "zombie" auto-blocks
- no "missing" auto-blocks
- `last-enriched` < 90 days old
- `content-readiness: ready`

**Observed reality of the queue:**
- 125 profiles stamped total
- 72 donors
- 50 corporations
- 3 "other" (2 sub-notes, 1 unknown)
- **0 politician master profiles**
- 413 politician master profiles are `content-readiness: ready` but unstamped — because they actually face real quality checks and fail

**What the UI tells David:** "125 profiles passed every A+ check and are waiting on your editorial sign-off."

**Actual meaning:** "122 non-politician profiles passed a very weak check; 0 politicians passed the real check; you've been staring at a queue of non-politician profiles while the politician profiles — the ones with the highest defamation risk and editorial complexity — aren't represented at all."

This is THE pattern David described: checklist reports things as "ready" that aren't actually verified.

### P-008 (STALE vocab): "S-tier" in signoff-queue UI
`ops/src/app/signoff-queue/page.tsx:85-86, 128-131, 260-266, 294-296` references S-tier
promotion. ADR-0017 (2026-04-23) rewrote the readiness flow as
`raw → draft → ready → data-complete → verified`. S-tier is not in the current
vocabulary. The page's "S-tier ready" column + footer copy are drift from the
current tier system.

### P-009 (STALE logic): "pipeline-janitor --tier=a-plus" empty-state instructions
Same page at line 192-195 tells David to run:
`node scripts/pipeline-janitor.cjs --tier=a-plus --cohort --write`
But per ADR-0017 the new readiness pipeline is `scripts/reclassify-readiness.cjs`.
This empty-state instruction may be pointing at obsolete tooling.

### P-010 (BLOCKING): janitor's `audit-a-plus-passed` stamp is one-way
Once stamped, the field is never cleared automatically. If a profile later
develops problems (prose-data mismatch, URL rot, source downgrade), the stamp
stays. The only way off the queue is editorial sign-off. A profile that was
A+ in February can be broken today and still look "waiting on David's sign-off."

### P-011 (BLOCKING): the janitor doesn't check prose-vs-structured-data mismatches
David: "Currently we keep finding mistakes with the numbers" — prose says
one dollar amount, auto-block graph says another. The janitor checks:
- Is the auto-block present? ✓
- Is the frontmatter field set? ✓
- Does the body sum/restate match the auto-block data? ✗ (not checked)

So a profile saying "ADM gave $2.4M" in prose while its auto-block shows
$1.8M passes A+ today. This is a gap in the checker, not a bug in the stamp.

---

## Verdict on sign-off queue

The queue isn't wrong about WHO is on it — those 125 profiles do have the
stamp. But the stamp doesn't mean what the UI claims it means. The mental model
"passed every A+ check → just needs sign-off" is false for 122 of 125 entries.

**Fix shape (for later discussion):**
1. Remove the stamp for non-politicians OR build A+ checks for donors/corporations too.
2. Fix the UI copy + readiness-tier vocabulary to match ADR-0017.
3. Add the prose-vs-structured-data check that catches the "numbers don't match" class of bugs.
4. Make the stamp reversible — nightly re-audit that clears stamps on profiles that drifted.

---

### /signoff-launch trace — same disease, different body

**Data flow:**
1. `/signoff-launch` page reads `/api/launch-50`.
2. API reads `content/Admin Notes/launch-50-audit.json` (audit) + `ops/data/launch-50-signoff.json` (user checkbox state).
3. Audit is generated by running `node scripts/launch-50-audit.cjs` manually.
4. Page shows 4-column progress bar: Class Analysis, URLs Verified, David Reviewed, Verified.

**P-012 (BLOCKING): audit JSON is a frozen snapshot, not a live check.**
The audit file captures `readiness`, `sourceCount`, `urlNeeded`, `unverified`, `needsReview`,
`hasClassAnalysis`, `autoBlocks` at the moment `launch-50-audit.cjs` runs. Nothing
auto-refreshes. Page displays whatever was true at last audit run, no matter
how old. No timestamp shown in the UI.

**Confirmed concrete lie:** audit JSON (generated today 2026-04-23 12:22 PM)
reports Kamala Harris as `readiness: "draft"`, `sourceCount: 0`. Actual current
frontmatter: `content-readiness: data-complete`, has `source-types: [Congress, ...]`.
The checklist is showing stale state. David looking at the page right now
would see Kamala blocking on things she's already past.

**P-013 (BLOCKING): 3 of 4 checkboxes are manual toggles stored in a gitignored file.**
`ops/data/launch-50-signoff.json` — per route.ts:7. Verified the directory doesn't
even exist yet (`ls ops/data/` returned "No such file or directory"). This means:
- Progress is lost on machine switch
- Progress not committed to git, so ANY backup/restore wipes it
- If the worktree is deleted, progress is gone
- No audit trail of who toggled what when (just an `updatedAt` timestamp)

**P-014 (BLOCKING): manual checkbox overrides audit data.**
`signoff-launch/page.tsx:103,111,147` — `hasCA = s.classAnalysisWritten || r.hasClassAnalysis`.
If the audit finds no Class Analysis heading, but David ticks the checkbox anyway,
the dashboard goes green. Nothing stops false-positive checkmarks. The "Class
Analysis" box was intended to be automatic (from audit) but made manually-overridable.

**P-015 (BLOCKING): status calculation ignores readiness tier.**
`signoff-launch/page.tsx:108-116` — a profile at `readiness: draft` can be marked
`ready-to-sign` if David ticks all the checkboxes. The status function only
checks: has CA + urls verified + no flags. Nothing stops a draft-tier profile
from reporting ready-to-sign. (Exception: `promoted` checkbox OR `readiness===verified`
→ 'done'. But the path to "ready-to-sign" doesn't block on readiness.)

**P-016 (BLOCKING): different source-count logic than sign-off queue.**
- sign-off queue's janitor (`pipeline-janitor.cjs:301`): `Math.max((data['source-types'] || []).length, countTier1InBody(body))`
- launch-50 audit (`launch-50-audit.cjs:114`): `(fm["source-types"] || []).length` — NO body fallback.

The two checklists count sources differently. A profile with Tier 1 source
refs in prose but empty frontmatter `source-types` would show zero sources
on launch-50, some sources on sign-off queue. Same profile, two different
truths, and no indication to the user which is right.

**P-017 (UX): hasClassAnalysis is just a heading regex.**
`launch-50-audit.cjs:113` — `/^##\s+Class Analysis/m.test(body)`. A profile
with an empty `## Class Analysis` heading passes. Short, useless content passes.
Doesn't verify quality, length, structured nature, or whether the required
sub-sections exist (per Class Analysis rubric in ADR-0001).

**P-018 (ORPHAN): launch-50-audit.cjs hardcodes the 50 names in source.**
`launch-50-audit.cjs:21-47` — the LAUNCH_50 constant is a hardcoded JS array.
Changing the 50 requires editing the script and running it again. The ops UI
has no way to add/remove profiles from the launch list.

**P-019 (UX): the launch-50 page is not in the sidebar.**
Duplicate of P-003 — reconfirmed during trace. Launch-50 signoff is the most
stake-having surface in the app and requires URL-typing to reach.

**P-020 (UX): findProfile() uses fuzzy match that can resolve to wrong file.**
`launch-50-audit.cjs:86-103` — if a name doesn't match exactly, falls back to
partial match with heuristics ("prefer Master Profile", "prefer non-sub-note").
For a name like "George Soros" with multiple matching files (a donor profile
and a children-funded-by page), it could silently pick the wrong one and
audit the wrong profile. No warning surfaced to the UI when this happens.

---

## Verdict on launch-50

Same disease as sign-off queue but mechanically worse:
- Stale snapshot data (not live — has to be manually re-run)
- Manual checkboxes override audit (you can tick anything)
- Progress stored in gitignored file outside vault (can disappear)
- No readiness-tier gate
- Different source-count logic than the other checklist — the two disagree

David's quote applies literally: "I'm always having to go to claude to help me
fix the check lists because it may state something is wrong when it isn't or
vice versa." Kamala Harris example confirmed: checklist says draft/0 sources;
reality says data-complete/1+ sources.

---

## Other findings surfaced during ongoing work

### P-021 (BLOCKING): corrupted `central-thesis` via botched sed replacement
Discovered 2026-04-23 during Rule 9 investigation. File:
`content/Politicians/Democrats/Governors/Amy Acton/_Amy Acton Master Profile.md:5`
central-thesis contains garbage: "...96% of contributions under
content-readiness: ready00, $28 average donation..." — the phrase "content-readiness: ready"
(with a trailing 00) was injected into the middle of a dollar figure. Looks like a
sed/regex replacement script incorrectly pattern-matched on "under $" and mangled
the value.

**Class of bug:** sed-based bulk rewrite hit an unintended match. Should have a
harness check for "content-readiness:" or other vocabulary appearing WITHIN a
frontmatter value (not at the start of a key). Add to Phase 3 harness checks.

### P-022 (BLOCKING): 19 profiles still have dead Dataview `field::` trailers
Per comment in `ops/src/app/api/profile/readiness/route.ts:11`, a 2026-04-10
cleanup sweep "removed 562 files that had the legacy pattern" — but 19 survived.
All are politician master profiles with redundant `office::`, `state::`, `party::`
fields that already exist in frontmatter. Will be cleaned during this session's
Memory #19 sentinel rollout.

### P-023 (STALE): ops readiness API accepts "s-tier" as a valid tier
`ops/src/app/api/profile/readiness/route.ts:26` — VALID_TIERS includes "s-tier"
with an S-tier-specific promotion gate (lines 53-79). Per ADR-0017, S-tier no
longer exists; top tier is `verified`. This API is out of sync with the tier
vocabulary. Part of Rule 9 enforcement deferred session.

### P-024 (STALE): session-save skill uses old model name in commit template
`~/.claude/skills/session-save/skill.md:51` — commit template uses "Claude Opus 4.6"
in the Co-Authored-By line. Should be "Claude Opus 4.7" (or current model).
Minor drift. Fix during any session that touches the skill.


## /attention page audit (2026-04-23 evening, Code Claude)

Traced `ops/src/app/attention/page.tsx` + `ops/src/app/api/attention-queue/route.ts`
+ `scripts/attention-dispatcher.cjs` + `scripts/lib/attention-queue.cjs` +
`content/Admin Notes/.attention-queue-store.json` end-to-end.

Verdict: the /attention surface is functionally frozen in time. The dispatcher
daemon that's supposed to refresh it has never run. 118 of 122 current queue
entries are >24h stale; oldest (`missing-profile-detector`) is 13 days old. The
only source producing fresh entries is `vault-audit`, which was wired up today
in Phase 2. Everything else is a manually-run snapshot from whenever David last
invoked it by hand.

### P-025 (BLOCKING): Attention dispatcher has never run
`scripts/attention-dispatcher.cjs` defines 10 producer schedules (voice-drift
every 30m, hallucination-catcher hourly, promotion-candidate-queue hourly,
contradiction-miner 2hr, missing-profile-detector 2hr, relationship-discovery
4hr, narrative-drift 4hr, financial-disclosures daily 6am, bidirectional
normalizer + top-N weekly Sunday). Dispatcher writes to a log at
`content/Admin Notes/.attention-dispatcher.log`. **That log file does not exist.**
No rotated `.log.1` either. The daemon has never been started on this machine.

Downstream effect: every user-facing claim in CLAUDE.md's "Attention Queue"
section ("Background producers... write to a ranked queue David reads at
/attention", with specific cadences per producer) is aspirational, not
operational. The queue David reads is a manual snapshot graveyard.

Evidence: all entries outside `vault-audit` are >24h old:
- voice-drift-detector: 30/30 entries > 24h (claims 30-min cadence)
- hallucination-catcher: 25/25 > 24h (claims hourly)
- contradiction-miner: 15/15 > 24h (claims 2-hr)
- missing-profile-detector: 15/15 > 24h (claims 2-hr; entries 13 days old)
- promotion-candidate-queue: 10/10 > 24h (claims hourly)
- story-candidate-scorer: 20/20 > 24h (10 days old)
- narrative-drift-detector: 3/3 > 24h (7 days old)

Root cause: `scripts/attention-dispatcher.bat` exists with instructions to
copy a shortcut to `shell:startup`, but was never installed. No Windows
scheduled task, no systemd unit. Requires David to manually install the
shortcut OR switch to Windows Task Scheduler for survival across restarts.

### P-026 (BLOCKING): `lastUpdated` in API lies about queue freshness
`ops/src/app/api/attention-queue/route.ts:74` returns
`lastUpdated: stat.mtime.toISOString()` — the filesystem mtime of the
`.attention-queue-store.json` file. UI header renders "last updated 69 min ago"
from this value.

The mtime updates on every write from ANY producer — so if vault-audit writes
at 2:37 and voice-drift's entries are 3 days old, the header still says
"69 min ago" as if the whole queue is 69min fresh. It silently implies
currency the data doesn't have.

Fix: report per-source `lastUpdated` and/or take `max(created)` across all
current entries; show a warning chip when any source is past its claimed
cadence + grace period.

### P-027 (UX): No per-entry staleness shown in list
`page.tsx:286-346` renders each entry with `what`, `why`, `where`, leverage
stars, cost minutes, source — but not the `created` timestamp. A 13-day-old
entry renders identically to a 10-minute-old one. David has no way to
visually distinguish "this came from a producer that ran this morning" from
"this came from a producer that hasn't been invoked in two weeks."

Fix: show `created` as a relative age per entry ("2h ago" / "3d ago" /
"13d ago — producer hasn't re-run"), and optionally dim or flag entries
past their source's expected cadence.

### P-028 (BLOCKING/DISCIPLINE): Dispatcher has no auto-startup install
The `.bat` file at `scripts/attention-dispatcher.bat` documents the manual
"copy shortcut to shell:startup" procedure but no install check or smoke
test exists. Nothing in `/system-health` reports "dispatcher last alive".
Nothing in the ops app warns if the log file is absent or stale. The
failure mode for "daemon died silently" is the current state — zero
visibility.

Fix: add a `dispatcher-alive` check to `scripts/vault-audit.cjs` (harness
surfaces it in /system-health + /attention). Check is cheap: read
`.attention-dispatcher.log`, confirm mtime < 1 hour. If missing or stale,
flag as blocking. Bonus: install a Windows Task Scheduler registration
script so David doesn't have to navigate shell:startup manually.

### P-029 (STALE): Replacement semantics are correct but only fire on re-run
`scripts/lib/attention-queue.cjs:113` — `addEntries(sourceName, entries)`
does REPLACE semantics per source, so a producer that runs and finds 0
issues correctly clears its prior entries. This is the right design. But
because the dispatcher hasn't run, this self-cleaning property never
activates. A profile David fixed on 2026-04-22 can still show a
voice-drift entry from 2026-04-21 because voice-drift hasn't re-run to
confirm the issue is gone.

Fix: same as P-025 — get the dispatcher running. Once it is, this
property takes care of itself.

### P-030 (UX): "How this queue gets populated" footer lists 5 scripts, dispatcher has 10
`page.tsx:350-377` footer block teaches the user "re-run the scripts any
time: node scripts/voice-drift-detector.cjs etc." It names 5 producers.
The dispatcher actually manages 10. The footer's mental model is
"you run these scripts manually" — which is actually accurate for the
current broken state, but contradicts CLAUDE.md's claim that the
dispatcher handles scheduling. Pick one story: either the dispatcher
runs and the footer says "the dispatcher handles this on a schedule",
or it doesn't and the footer lists all 10 so the user knows what to
run.

## Dashboard + sidebar visual audit (2026-04-24 late morning, Code Claude)

David screenshotted the Dashboard, /pipelines, and sidebar. Quick
visual-level pass, not deep code trace.

### P-039 (STALE): Dashboard "S-TIER INSIGHTS" section header

Per ADR-0017, S-tier no longer exists (replaced by the 5-tier raw →
draft → ready → data-complete → verified flow). The whole S-TIER
INSIGHTS section + its sub-cards use retired vocabulary. Already
flagged as P-023 for the API layer; the Dashboard header is the
visual twin that hasn't been addressed.

### P-040 (BLOCKING): Dashboard "Ready for sign-off: 124" probably lies

The 2026-04-23 deep-trace of /signoff-queue found zero politicians
pass the real A+ bar; all 125 items in the queue were donors/
corporations that skipped the politician-only check. This Dashboard
number almost certainly reflects that same broken count. Verify
after the ADR-0022 pipeline-janitor universal-checks land on the
queue computation.

### P-041 (BLOCKING): Dashboard "Both-sides conflicts: 0" probably lies

The pipeline-janitor `a-plus-both-sides` check flagged 11 profiles in
its most recent harness run. The Dashboard showing "0" contradicts
the harness. Data source for this card is unknown — needs tracing.

### P-042 (UX): Sidebar is a 30-entry flat list, no hierarchy

Suggested grouping (carried to next session):
```
DAILY: Dashboard, Attention Queue, Sign-off Queue, Profile View,
       Source Registry, System Health
WEEKLY: Calendar, Pipelines, Relationships, Scripts, Bugs & Deferred
EXPERIMENTAL (collapsed): Publisher, Money Trail, Capitol Trades,
       Distribution, Class Tags, Policies, Connections, Contradictions
ADMIN: Operations, System Docs, Rulebook, Source Hunter, URL Manager,
       Editor, Query Engine, Ask, Notes & Queues, Alerts, Public Tips,
       Account, Pricing
```

The split mirrors the categorization already in CLAUDE.md's Ops app
section — just never applied visually.

### P-043 (STALE): Sidebar badge counts unaudited

`Notes & Queues (58)` and `Public Tips (1)` show badge counts with
unknown data sources. May be counting stale / false-positive
entries. Audit each badge's underlying query before trusting it.

---

## /pipelines page audit (2026-04-24 morning, Code Claude)

Traced `ops/src/app/pipelines/page.tsx` + `ops/src/app/api/pipelines/route.ts` +
`ops/src/app/api/pipeline-health/route.ts` + components (EnrichmentPanel,
PipelineRunHistory, ProfileEnrich, EnrichmentHistory, EnrichmentLog)
end-to-end. Also spot-checked the external repo the page triggers.

Verdict: the page works — it really does trigger GitHub Actions and
really does count enrichment commits — but the **catalog of pipelines it
displays is disconnected from reality in three different ways**, and the
page gives you no way to tell a retired pipeline from a broken one from
a scheduled-but-dormant one. Your own instinct from 2026-04-23 ("pipelines/
scripts aren't running right — everything downstream is built on bad
ground") is confirmed: no enrichment has run for 6 days (last: 2026-04-18),
and nothing in the ops app surfaces that as a blocking alert.

### P-031 (STALE): Pipeline Overview Grid disagrees with Pipeline Health

`page.tsx:52-60` hardcodes an 8-pipeline grid (FEC, Congress, GovTrack,
LobbyView, Committee, Relationships, Federal Register, USASpending) with
per-card Run buttons.

`pipeline-health/route.ts:28-59` has a `PIPELINE_LABELS` map with **29
pipelines** the health panel tracks (fec, fec-summary, congress, committee,
govtrack, lda, lobbyview, lobbying-contrib, usaspending, usaspending-awards,
sam, nonprofit-990, federal-register, fara, ofac-sdn, recall, nhtsa-recalls,
courtlistener, sec-edgar, sec-litigation, doj-press, propublica,
opensanctions, wikipedia, fcc, gleif, fda-enforcement, occ-enforcement,
ftc-enforcement, auto-connect).

Two hardcoded lists. 8 vs 29. No single source of truth. A pipeline
visible on the grid (e.g. Relationships, Committee) isn't in the health
panel's label set, so its activity is invisible on the health side.

Fix: one registry file (e.g. `ops/src/lib/pipeline-registry.ts`) that both
the grid and the health panel read. Each entry carries metadata: name,
label, status (`active` / `retired` / `experimental`), trigger mode
(`github-actions` / `local-script` / `manual`), expected cadence.

### P-032 (BLOCKING): API targets external repo that may be stale

`pipelines/route.ts:4-5` hardcodes:
```
const ENGINE_REPO = "donor-map-engine"
```

Triggers `api-enrichment.yml` on GitHub Actions in that repo. The repo
**exists** (private, verified via `gh repo view`) but `pushedAt` is
2026-04-15 — 9 days stale. The workflow may or may not still work.

No health check tells David "the engine repo is stale" or "the workflow
hasn't run since date X." Clicking Run on any pipeline card fires a
dispatch into that repo blindly.

Fix: the GET endpoint should fetch the engine repo's `pushedAt` + the
workflow's `last_run_at` and surface both on the page. If either is
stale, flag it.

### P-033 (BLOCKING): CLAUDE.md pipeline claims don't match git log

CLAUDE.md Rule 3 says:
> Three API pipelines remain active... Congress.gov, GovTrack, RSS
> digests. Every other API pipeline has been retired.

Git log of main repo, last 30 days, `API Enrichment Bot` commits (the
account all pipeline commits land under): **247 commits**. Sample:
- `2026-04-18 ... federal-register:9`
- `2026-04-18 ... gleif:4 nhtsa-recalls:7 ofac-sdn:22 recall:1 stock-watcher:20`

So gleif, nhtsa-recalls, ofac-sdn, recall, stock-watcher,
federal-register **have all run in the last 30 days**, despite CLAUDE.md
declaring them retired. CLAUDE.md is out of sync with reality.

Either CLAUDE.md is wrong (the pipelines actually ARE active and should
be listed) or the engine repo is still running retired pipelines by
accident (which wastes API quota + produces mystery commits).

Fix: reconcile. Derive the true "active pipelines" list from the last
30 days of commits, cross-check with what the engine's workflow actually
dispatches, update CLAUDE.md Rule 3 accordingly.

### P-034 (BLOCKING): 6 days of silent enrichment drift

Last enrichment commit on v4: 2026-04-18 23:00 UTC. Today: 2026-04-24
morning. Six days of no enrichment with zero ops-app surface flagging it.

The page tells you last-run-per-pipeline if you know which table to
read. But the **overall "nothing has enriched in 6 days"** alert isn't
anywhere — not on the Dashboard, not on /system-health, not on
/attention.

Fix: add an `enrichment-freshness` harness check. Read the most-recent
`API Enrichment Bot` commit date from `git log`. If >3 days old during
an active sprint, flag blocking. Route to /attention automatically via
the existing harness queue wiring.

### P-035 (UX): "Dead" status is ambiguous

`pipeline-health/route.ts:126-128`:
```
if (runsLast7d > 0) status = "healthy"
else if (runsLast30d > 0) status = "stale"
else status = "dead"
```

"Dead" means "no commits in 30 days." That could be:
- pipeline retired on purpose (expected)
- pipeline broken (needs fix)
- pipeline scheduled but hasn't fired yet (fine, just hasn't run)
- pipeline's engine repo workflow disabled

No way to distinguish from the UI. Every dead pipeline looks the same.

Fix: add an explicit `retired: true` flag to the registry entries
proposed in P-031. A "dead" pipeline that's marked retired renders gray
+ filtered out by default. A "dead" pipeline NOT marked retired renders
red + surfaces as a /attention blocking item.

### P-036 (UX): PipelineRunHistory + EnrichmentHistory + EnrichmentLog
three near-identical "recent activity" surfaces

The page renders three history surfaces:
- `PipelineRunHistory` — GitHub workflow runs
- `EnrichmentHistory` — git commit log of enrichment changes
- `EnrichmentLog` — detailed per-profile results

Different data sources, overlapping purposes. A normie opens the page
and sees three "recent runs" lists side by side. Which is authoritative?
Which surfaces what?

Noting for later — not audited in depth this pass. Likely a consolidation
opportunity, but lower priority than P-031–P-034.

### P-037 (BLOCKING): EnrichmentPanel has its OWN hardcoded 32-pipeline list

The pipeline registry fix (P-031) wired the top 8-card grid to
`ops/src/lib/pipeline-registry.ts`. But the `EnrichmentPanel` component
(rendered under "Bulk Enrichment" tab) has an **independent** hardcoded
list of 32 pipelines grouped into AUTO-FILL / SOURCE DISCOVERY /
NEEDS REVIEW / RELATIONSHIP categories. After the 2026-04-24 pause,
that panel still cheerfully offers Run buttons for disabled workflows.

Fix: wire EnrichmentPanel (and its sibling ProfileEnrich) to read from
PIPELINE_REGISTRY. Dim/disable paused entries. Consolidate the
category labels (AUTO-FILL/SOURCE DISCOVERY/etc.) into the registry as
per-pipeline metadata so there's one catalog.

### P-038 (DISCIPLINE): Dev-server hot-reload doesn't pick up new lib files

New modules added under `ops/src/lib/` (like pipeline-registry.ts)
sometimes don't hot-reload in Next.js dev. The /pipelines page was
still rendering the old 8-card grid even after the registry was
committed — David had to manually restart `npm run dev`.

Fix options: (a) document the restart in ops/README, (b) use Turbopack
(`npm run dev -- --turbo`), (c) add npm script `restart-ops` for
convenience.

### Verdict on /pipelines

The page is functional infrastructure with a stale catalog layer on
top. The buttons do fire GitHub Actions. The health counts do read real
commits. But:

1. The list of pipelines you see is not the list of pipelines that run.
2. The engine repo they target hasn't been pushed to in 9 days.
3. CLAUDE.md's claim about which pipelines are active is wrong.
4. Six days of enrichment drought is invisible on the page.

**Highest-leverage fix:** the pipeline registry (P-031) +
enrichment-freshness harness check (P-034). Together they give you one
truth for "what pipelines exist" and one alert for "enrichment stopped."

### Verdict on /attention

Every finding reduces to P-025: **the dispatcher is not running**. Once the
daemon is alive on David's machine, the replacement semantics + fresh
entries handle most of P-026, P-027, P-029. The surface is well-designed;
the infrastructure behind it was never turned on.

**Highest-leverage fix:** install the dispatcher. Add a dispatcher-alive
harness check that surfaces in /system-health so silent-death is visible.
Then the other UX polishes (per-entry age, per-source lastUpdated) are
small follow-ups.

